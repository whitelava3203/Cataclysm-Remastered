using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class CursorFollowerController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        Vector3 v = Camera.main.ScreenToWorldPoint(Input.mousePosition);
        v.z = 0;
        gameObject.transform.position = v;
        if(Mod_ChunkMaker.상태 == Mod_ChunkMaker.E상태.선택안되있음)
        {
            gameObject.GetComponent<SpriteRenderer>().sprite = Mod_ChunkMaker.현재타일.이미지로드();
        }
        else if(Mod_ChunkMaker.상태 == Mod_ChunkMaker.E상태.타일선택됨)
        {
            gameObject.GetComponent<SpriteRenderer>().sprite = null;
        }
    }
}
*/